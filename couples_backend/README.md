# Couples Backend

Minimal shared backend for the Flutter couples app.

## Endpoints

- `POST /bootstrap-user`
- `POST /bind-couple-by-pair-code`
- `POST /chat/list`
- `POST /chat/send`

## Quick start

```bash
copy .env.example .env
npm.cmd install
npm.cmd run init-db
npm.cmd run dev
```
